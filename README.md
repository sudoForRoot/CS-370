# CS-370
An intelligent agent trained using reinforcement learning (Q-learning) and neural networks to navigate a grid-based environment and locate treasure. This project demonstrates core concepts in AI, including exploration vs. exploitation, reward-based learning, and deep Q-networks (DQN).
